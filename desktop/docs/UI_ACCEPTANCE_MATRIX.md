# 前端页面职责与上线验收矩阵

本文档是桌面端工作台模块化改造的上线合同。页面、按钮和数据请求都必须遵守这里的边界；新增功能时先选择所属页面，不能继续向全局工作台堆叠。

## 1. 页面设计原则

1. 一个 URL 只承载一个完整用户目标，不按“一个按钮一个页面”机械拆分。
2. 页面内可以包含完成同一目标所需的步骤；跨业务域动作只展示摘要并导航到目标页面。
3. DOM 只挂载当前页面；页面卸载时必须停止本页轮询、请求和全局监听。
4. 每页独立处理 `loading / empty / error / permission denied / ready`，API 失败不得伪装成空列表或演示数据。
5. 页面头最多一个主操作。批量、危险、付款、发送、停用等动作必须给出范围、身份、后果和确认。
6. 生产模式不得显示演示任务、错误窗口、失败演示、超时演示等开发工具。

## 2. 路由与按钮职责

| 页面 | 唯一职责 | 页面内主操作 | 允许的辅助操作 | 不得混入 |
| --- | --- | --- | --- | --- |
| `/overview` | 查看全局经营与通道状态 | 刷新总览 | 打开会话、接入、发送、审核等目标页 | 直接执行自动化、发送、审批 |
| `/conversations` | 筛选和选择客户会话 | 打开会话 | 账号、通道、未读和 SLA 筛选 | 设计、报价、订单编辑 |
| `/conversations/[id]` | 处理一条客户会话 | 人工回复入队 | 已读、接管、分配、优先级、SLA；查看其他域摘要 | 直接出图、报价、收款、生产 |
| `/routing` | 评估或纠正消息路由 | 判断谁来处理 | 选择会话、输入消息、查看场景/价值/缺项证据 | 发送消息、设计、报价 |
| `/send/queue` | 处理可执行发送队列 | 安全处理队列 | 定位会话/订单、快照校验 | 错误窗口和演示发送 |
| `/send/blocked` | 处理被拦截或不确定的发送 | 解除明确阻塞并重排 | 查看阻塞证据、取消任务、人工接管 | 未知状态自动重试 |
| `/send/diagnostics` | 诊断适配器、窗口和回执 | 刷新诊断 | 查看失败尝试、实时校验 | 直接绕过守卫发送 |
| `/integrations/channels` | 查看接入通道状态 | 刷新通道 | 打开对应配置或验收页 | 会话、发送和业务自动化 |
| `/integrations/wechat-work` | 企业微信本地配置与上线预检 | 运行只读预检 | 查看缺项、刷新状态、入站验收 | 将本地预检宣称为平台验收通过 |
| `/integrations/personal-wechat/instances` | 管理个人微信 RPA 实例 | 保存实例 | 校验、停用、查看端点与账号 | 暴露 token、自动重试不确定发送 |
| `/integrations/personal-wechat/control` | 查看账号/Windows 会话/窗口隔离 | 刷新账号控制面 | 人工接管、审批状态 | 隐式切号、跨账号发送 |
| `/integrations/personal-wechat/window-inbound` | 验证真实窗口与受控入站链路 | 采集当前窗口 | 快照入库、身份绑定入站验证 | 无身份入站、自动外发 |
| `/integrations/personal-wechat/safety` | 查看同意、频控、敏感内容与审计 | 全局停止 | 账号预算、人工复核、未知投递隔离 | 规避平台限制、模拟真人身份 |
| `/design/settings` | 配置设计平台连接 | 保存配置 | 清凭证、健康检查、回调状态、试跑 | 设备激活、账号登录、素材和任务 |
| `/design/activation` | 绑定本机设计设备 | 激活设备 | 生成/填写设备 ID、填写后台激活码 | 账号登录、连接参数和任务 |
| `/design/account` | 登录设计平台账号 | 登录账号 | 填写账号、密码和已激活设备 ID | 设备激活、连接参数和任务 |
| `/design/assets` | 上传并管理客户素材 | 上传素材 | 选择、绑定设计任务、预览 | 设计任务状态推进 |
| `/design/jobs` | 筛选和创建设计任务 | 新建设计任务 | 状态筛选、批量轮询 | 商品和报价编辑 |
| `/design/jobs/[id]` | 完成一条设计任务 | 提交或重试当前任务 | 预检、轮询、选图、改图、取消 | 跨客户选图、直接付款处理 |
| `/catalog/products` | 管理商品资料 | 新增商品 | 筛选、编辑、上下架、批量修改 | 图片修复队列和导入向导 |
| `/catalog/repair` | 处理资料与图片问题 | 保存当前修复 | 严重度/路径筛选、复制交接、导出 | 商品批量导入 |
| `/catalog/import` | 完成商品导入向导 | 确认入库 | 文件选择、字段映射、预览、校验 | 在线商品编辑和报价 |
| `/catalog/audit` | 查看商品库可自动化程度 | 刷新体检 | 库存、利润、规格、交期、图片问题筛选 | 直接修改设计或订单 |
| `/catalog/bundles` | 验证预算/场景搭配 | 生成搭配建议 | 查看组合依据和阻塞项 | 自动承诺库存或交期 |
| `/sales/actions` | 在报价与订单两个独立流程之间选择目标 | 打开目标流程 | 查看两个流程的职责边界 | 在同页混合报价和订单状态机 |
| `/sales/quotes` | 管理报价 | 新建或发送报价 | 搜索、筛选、修订选图、付款凭证核验 | 订单生产/交付状态 |
| `/sales/quotes/[id]` | 处理一条报价 | 发送报价或建单 | 预览话术、修订、核验定金/全款 | 跳过身份与付款证据 |
| `/sales/orders` | 管理订单跟进 | 打开待处理订单 | 搜索、阶段、付款和下一步筛选 | 报价批量发送 |
| `/sales/orders/[id]` | 推进一条订单 | 执行当前可用下一步 | 确认、收款、生产、交付、取消、记录跟进 | 跳阶段或跨客户操作 |
| `/automation/runs` | 控制和查看自动化运行 | 运行一轮 | 启停、查看阶段与最近运行 | 直接修改训练样本 |
| `/automation/issues` | 处理自动化卡点 | 处理第一项 | 错误、缺字段、发送对象、人工接管筛选 | 静默忽略失败 |
| `/notifications` | 阅读和定位提醒 | 全部已读 | 单条已读、定位目标 | 执行跨域危险动作 |
| `/agents` | 查看 Agent 与适用范围 | 打开 Agent | 场景、状态和能力筛选 | 直接应用未审核 Skill |
| `/training/import` | 导入对话训练材料 | 导入训练 | 预览、批次查看、字段检查 | 样本最终审核 |
| `/training/review` | 复核训练样本 | 确认所选样本 | 缺项/风险/场景筛选、编辑、退回、禁用 | 自动确认风险样本 |
| `/training/skills` | 审核和应用 Skill 建议 | 应用已审核建议 | 可信度、Agent、证据筛选 | 应用被阻塞或跨范围建议 |
| `/reviews/inbox` | 处理人工接管队列 | 处理第一项 | 会话、阻塞发送、接管时间筛选 | 设计/报价/订单审批 |
| `/reviews/design` | 审核设计结果 | 批准或要求改图 | 定位任务、批准发送、驳回 | 报价与订单审批 |
| `/reviews/quotes` | 审核报价 | 通过并入队 | 核验付款、继续跟进、驳回 | 设计选图 |
| `/reviews/orders` | 审核订单确认与跟进 | 审核当前动作 | 交期、付款、人工记录、驳回 | 报价生成 |
| `/reviews/logs` | 查询审核轨迹 | 刷新记录 | 类型、操作者和时间筛选 | 修改业务状态 |
| `/settings/access` | 查看操作者身份与角色能力 | 刷新权限 | 查看 readiness 和能力矩阵 | 在前端授予后端权限 |

## 3. 按钮合同

- 每个业务按钮必须有稳定的 `data-action-id`，名称使用 `<domain>.<entity>.<verb>`。
- 按钮文案必须描述结果，例如“发送报价”不能写成含糊的“处理”。
- 禁用按钮必须同时提供可见原因或可访问说明，不能只改变颜色。
- 跨模块按钮只调用路由，不得在当前页偷偷执行目标模块动作。
- 发送、付款、停用、取消、驳回、批量修改必须显示确认范围和身份。
- 正在执行时只锁定当前动作或当前实体，不得用全局 `busy` 禁用整个工作台。
- 不确定投递、身份不一致、权限未确认和配置缺失均按 fail-closed 处理。

## 4. 代码模块合同

每个 feature 目录至少包含：

```text
features/<domain>/
  api.ts              # 只封装本域现有接口
  model.ts            # 页面模型和状态类型
  selectors.ts        # 纯派生逻辑，可独立测试
  use-*-controller.ts # 本页请求、动作和清理
  *-feature-page.tsx  # 页面组合层
  components/         # 纯展示积木
  *.module.css        # 本域样式
```

约束：

- `app/**/page.tsx` 直接导入且只导入一个归属 feature，或执行一个明确重定向；业务逻辑留在 feature，公共壳由 `app/feature-route-shell.tsx` 提供。
- feature 不得直接修改共享导航、全局 token、旧 `page.tsx` 或 `globals.css`。
- 共享 Context 只保存账号、会话、客户与操作者身份，不保存跨域业务数据。
- 每个 controller 只加载本页接口，并在卸载时取消或忽略过期请求。
- 新代码不得继续向未引用的 `legacy-workbench.tsx` 添加演示回退；新路由不得重新引入旧 `globals.css`。

## 5. 响应式与可访问性

- 验收宽度：1440、1280、1024、760 和 390px。
- 390px 不允许横向滚动、固定头遮挡、底部导航覆盖主操作或不可达按钮。
- 键盘必须可完成导航、筛选、确认和取消；弹层支持 Escape 并恢复焦点。
- 每页只有一个 `h1` 和一个主 `main` landmark；表格在移动端必须保持字段语义。
- 动画遵守 `prefers-reduced-motion`。

## 6. 上线门禁

每个分支至少通过：

1. 模块定向测试和现有相关契约测试。
2. TypeScript 检查和 Next 生产构建。
3. `git diff --check`。
4. 桌面与 390px 真浏览器检查：URL、标题、非空、无框架错误层、控制台、截图、一个核心交互。
5. 深链接、刷新、前进/后退、错误态、空态、权限态和长文本。
6. 证明切页后本模块轮询和事件监听已停止。

最终集成按 `router foundation -> feature branches -> route cutover -> browser QA` 顺序进行，禁止把主脏工作区当作集成分支。
